# ESP32硬件接线说明

## 📋 目录

- [引脚总览](#引脚总览)
- [模块详细接线](#模块详细接线)
- [接线注意事项](#接线注意事项)
- [接线示意图](#接线示意图)
- [电源管理](#电源管理)
- [常见接线错误](#常见接线错误)

---

## 引脚总览

### ESP32引脚功能分配表

| 序号 | 模块名称 | 接口类型 | ESP32引脚 | 说明 |
|------|----------|---------|-----------|------|
| 1 | **OLED SSD1306** | I2C | SDA=GPIO21, SCL=GPIO22 | 显示屏 |
| 2 | **MPU6050** | I2C | SDA=GPIO21, SCL=GPIO22 | 共享I2C总线 |
| 3 | **RC522 RFID** | SPI | MOSI=23, MISO=19, SCK=18, CS=5, RST=4 | 读卡器 |
| 4 | **AM2302 (DHT22)** | 1-Wire | DATA=GPIO15 | 温湿度传感器 |
| 5 | **HC-SR04** | GPIO | Trig=GPIO25, Echo=GPIO26 | 超声波测距 |
| 6 | **蜂鸣器** | PWM | GPIO27 | 声音报警 |
| 7 | **3W LED** | PWM | GPIO32 | LED照明 |
| 8 | **HW-269红外** | GPIO | GPIO33 | 人体检测 |
| 9 | **串口调试** | UART0 | TX=GPIO1, RX=GPIO3 | USB串口 |

### 引脚分配原则

✅ **可以使用的引脚**：
- GPIO0, GPIO2, GPIO4, GPIO5
- GPIO12-GPIO15
- GPIO16-GPIO19
- GPIO21-GPIO23
- GPIO25-GPIO27
- GPIO32-GPIO33

⚠️ **特殊用途引脚**（避免或谨慎使用）：
- **GPIO0**：BOOT按钮，启动时不能拉低
- **GPIO1/GPIO3**：串口TX/RX，烧录时占用
- **GPIO6-GPIO11**：连接Flash，**禁止使用**
- **GPIO34-GPIO39**：只能输入，无上拉，适合ADC
- **GPIO12**：启动时需要为低电平

❌ **禁止使用的引脚**：
- GPIO6, GPIO7, GPIO8, GPIO9, GPIO10, GPIO11（连接Flash）

---

## 模块详细接线

### 1. OLED SSD1306 (I2C接口)

**模块规格**：
- 尺寸：0.96寸
- 分辨率：128x64
- 接口：I2C (4针)
- I2C地址：0x3C（默认）

**接线表**：

| OLED引脚 | ESP32引脚 | 说明 |
|----------|----------|------|
| VCC | 3.3V | 供电（注意：**不是5V**） |
| GND | GND | 地 |
| SCL | GPIO22 | I2C时钟线 |
| SDA | GPIO21 | I2C数据线 |

**接线图**：
```
OLED SSD1306          ESP32
┌──────────┐         ┌──────┐
│ VCC      │────────►│ 3.3V │
│ GND      │────────►│ GND  │
│ SCL      │────────►│ G22  │ (I2C时钟)
│ SDA      │────────►│ G21  │ (I2C数据)
└──────────┘         └──────┘
```

**注意事项**：
- ⚠️ OLED供电必须是**3.3V**，接5V可能烧毁！
- ⚠️ I2C总线需要上拉电阻（大多数OLED模块已内置）
- ⚠️ 如果显示不正常，检查I2C地址（用扫描工具）

---

### 2. MPU6050 加速度计 (I2C接口)

**模块规格**：
- 6轴传感器（3轴加速度+3轴陀螺仪）
- 接口：I2C
- I2C地址：0x68（AD0=GND）或0x69（AD0=VCC）

**接线表**：

| MPU6050引脚 | ESP32引脚 | 说明 |
|-------------|----------|------|
| VCC | 3.3V或5V | 供电（模块有稳压器，两者都可） |
| GND | GND | 地 |
| SCL | GPIO22 | I2C时钟（与OLED共享） |
| SDA | GPIO21 | I2C数据（与OLED共享） |
| XDA | 不接 | 可选的从I2C |
| XCL | 不接 | 可选的从I2C |
| AD0 | GND | I2C地址选择（低=0x68） |
| INT | 不接（可选GPIO34） | 中断输出（可选） |

**接线图**：
```
MPU6050              ESP32
┌──────────┐         ┌──────┐
│ VCC      │────────►│ 3.3V │
│ GND      │────────►│ GND  │
│ SCL      │────────►│ G22  │ (与OLED共享)
│ SDA      │────────►│ G21  │ (与OLED共享)
│ AD0      │────────►│ GND  │ (I2C地址=0x68)
│ INT      │         │      │ (可选不接)
└──────────┘         └──────┘
```

**注意事项**：
- ✅ MPU6050与OLED共享I2C总线（通过地址区分）
- ✅ AD0接GND时地址为0x68，接VCC时为0x69
- ⚠️ 如果有两个I2C设备地址冲突，需要修改其中一个

---

### 3. RC522 RFID读卡器 (SPI接口)

**模块规格**：
- 频率：13.56MHz
- 通信距离：3-5cm
- 接口：SPI
- 支持卡片：Mifare S50/S70、NTAG等

**接线表**：

| RC522引脚 | ESP32引脚 | 说明 |
|-----------|----------|------|
| VCC (3.3V) | 3.3V | 供电（**必须3.3V**） |
| GND | GND | 地 |
| RST | GPIO4 | 复位引脚 |
| IRQ | 不接 | 中断（可选） |
| MISO | GPIO19 | SPI主入从出 |
| MOSI | GPIO23 | SPI主出从入 |
| SCK | GPIO18 | SPI时钟 |
| SDA (CS) | GPIO5 | SPI片选 |

**接线图**：
```
RC522                ESP32
┌──────────┐         ┌──────┐
│ VCC(3.3V)│────────►│ 3.3V │ ⚠️ 不能接5V！
│ GND      │────────►│ GND  │
│ RST      │────────►│ G4   │
│ IRQ      │         │  -   │ (不接)
│ MISO     │────────►│ G19  │
│ MOSI     │◄────────│ G23  │
│ SCK      │◄────────│ G18  │
│ SDA(CS)  │◄────────│ G5   │
└──────────┘         └──────┘
```

**注意事项**：
- ⚠️ **RC522只能用3.3V供电**，接5V会烧毁！
- ⚠️ SDA引脚实际是片选CS，不是I2C的SDA
- ⚠️ 天线部分不要接触金属物体
- ⚠️ 读卡距离太近或太远都会失败（最佳距离2-3cm）

---

### 4. AM2302 (DHT22) 温湿度传感器

**模块规格**：
- 温度范围：-40~80℃
- 湿度范围：0-100% RH
- 接口：单总线（1-Wire）
- 精度：±0.5℃，±2% RH

**接线表**：

| AM2302引脚 | ESP32引脚 | 说明 |
|------------|----------|------|
| VCC (+) | 5V或3.3V | 供电（5V更稳定） |
| DATA | GPIO15 | 数据线 |
| GND (-) | GND | 地 |

**接线图（3针版本）**：
```
AM2302               ESP32
┌──────────┐         ┌──────┐
│ VCC (+)  │────────►│ 5V   │
│ DATA     │────────►│ G15  │
│ GND (-)  │────────►│ GND  │
└──────────┘         └──────┘
      │
      └──────[4.7K上拉电阻]──► 5V (可选，模块可能内置)
```

**接线图（4针版本）**：
```
4针AM2302：
1. VCC  → 5V
2. DATA → GPIO15
3. NULL → 不接
4. GND  → GND
```

**注意事项**：
- ✅ 数据线需要4.7kΩ上拉电阻（部分模块已内置）
- ✅ 供电建议用5V（虽然3.3V也能工作，但5V更稳定）
- ⚠️ 读取间隔至少2秒，否则会出错
- ⚠️ 不要与强磁场或高温物体接近

---

### 5. HC-SR04 超声波测距模块

**模块规格**：
- 测距范围：2cm - 4m
- 精度：±3mm
- 工作电压：5V
- 接口：GPIO

**接线表**：

| HC-SR04引脚 | ESP32引脚 | 说明 |
|-------------|----------|------|
| VCC | 5V | 供电（**必须5V**） |
| Trig | GPIO25 | 触发引脚（输出10μs脉冲） |
| Echo | GPIO26 | 回响引脚（读取高电平时间） |
| GND | GND | 地 |

**接线图**：
```
HC-SR04              ESP32
┌──────────┐         ┌──────┐
│ VCC      │────────►│ 5V   │
│ Trig     │◄────────│ G25  │ (输出)
│ Echo     │────────►│ G26  │ (输入)
│ GND      │────────►│ GND  │
└──────────┘         └──────┘
```

**注意事项**：
- ⚠️ HC-SR04必须用**5V供电**，3.3V可能工作不正常
- ⚠️ Echo引脚输出是5V电平，ESP32是3.3V，理论上需要电平转换
  - 但大多数情况下可以直接连接（ESP32的GPIO输入可容忍5V）
  - 如果担心损坏，可以加分压电阻：
    ```
    Echo ─┬─[1KΩ]─► GPIO26
          └─[2KΩ]─► GND
    (分压后约3.3V)
    ```
- ⚠️ 测量间隔至少60ms
- ⚠️ 前方不能有遮挡物

---

### 6. 蜂鸣器

**模块类型**：
- **有源蜂鸣器**：接通电源就响（推荐）
- **无源蜂鸣器**：需要PWM信号驱动

**接线表**：

| 蜂鸣器引脚 | ESP32引脚 | 说明 |
|-----------|----------|------|
| + (VCC) | GPIO27 | 控制引脚 |
| - (GND) | GND | 地 |

**接线图（有源蜂鸣器）**：
```
有源蜂鸣器            ESP32
┌──────────┐         ┌──────┐
│ +        │◄────────│ G27  │
│ -        │────────►│ GND  │
└──────────┘         └──────┘

控制方式：
GPIO27=HIGH → 响
GPIO27=LOW  → 不响
```

**接线图（无源蜂鸣器，需PWM）**：
```
无源蜂鸣器            ESP32
┌──────────┐         ┌──────┐
│ +        │◄────────│ G27  │ (PWM输出)
│ -        │────────►│ GND  │
└──────────┘         └──────┘

控制方式：
输出2000Hz PWM → 响
停止PWM → 不响
```

**注意事项**：
- ✅ 推荐使用有源蜂鸣器（编程简单）
- ⚠️ 如果蜂鸣器声音太小，可能需要三极管驱动：
  ```
  GPIO27 ─[1KΩ]─► NPN三极管基极
  蜂鸣器+ ─► 5V
  蜂鸣器- ─► 三极管集电极
  三极管发射极 ─► GND
  ```

---

### 7. 3W白光LED模块

**接线表**：

| LED模块引脚 | ESP32引脚 | 说明 |
|-------------|----------|------|
| + (VCC) | GPIO32 | PWM控制（调光） |
| - (GND) | GND | 地 |

**接线图**：
```
3W LED模块           ESP32
┌──────────┐         ┌──────┐
│ + (IN)   │◄────────│ G32  │ (PWM)
│ - (GND)  │────────►│ GND  │
└──────────┘         └──────┘

PWM占空比：
0%   → 灭
50%  → 半亮
100% → 全亮
```

**注意事项**：
- ⚠️ 大功率LED模块通常内置恒流驱动，可以直接用GPIO控制
- ⚠️ 如果是普通LED，需要串联限流电阻：
  - 5V电源：330Ω电阻
  - 3.3V电源：220Ω电阻
- ⚠️ 长时间点亮注意散热

---

### 8. HW-269 人体红外传感器

**模块规格**：
- 检测距离：3-7米（可调）
- 延迟时间：5-200秒（可调）
- 输出：高电平（有人）/ 低电平（无人）

**接线表**：

| HW-269引脚 | ESP32引脚 | 说明 |
|------------|----------|------|
| VCC | 5V或3.3V | 供电（5V更灵敏） |
| OUT | GPIO33 | 信号输出 |
| GND | GND | 地 |

**接线图**：
```
HW-269               ESP32
┌──────────┐         ┌──────┐
│ VCC      │────────►│ 5V   │
│ OUT      │────────►│ G33  │
│ GND      │────────►│ GND  │
└──────────┘         └──────┘

输出信号：
有人 → OUT=HIGH (3.3V)
无人 → OUT=LOW  (0V)
```

**模块调节**：
- **灵敏度调节（左侧旋钮）**：
  - 顺时针：距离增加（最大7米）
  - 逆时针：距离减小（最小3米）

- **延时调节（右侧旋钮）**：
  - 顺时针：延时增加（最大200秒）
  - 逆时针：延时减小（最小5秒）

**注意事项**：
- ⚠️ 上电后需要1分钟预热稳定
- ⚠️ 避免对着空调、风扇等热源
- ⚠️ 检测范围是扇形，不是全向
- ⚠️ 只能检测移动的人体，静止不触发

---

## 接线注意事项

### 电压兼容性

| 模块 | 工作电压 | ESP32引脚耐压 | 是否需要电平转换 |
|------|---------|--------------|----------------|
| OLED SSD1306 | 3.3V | 3.3V | ✅ 直接连接 |
| MPU6050 | 3.3V/5V | 3.3V | ✅ 直接连接（I2C上拉到3.3V） |
| RC522 | **仅3.3V** | 3.3V | ✅ 直接连接 |
| AM2302 | 3.3V/5V | 3.3V | ✅ 直接连接（数据线上拉） |
| HC-SR04 Echo | 5V输出 | 3.3V输入 | ⚠️ 建议分压（但多数可直连） |
| 蜂鸣器 | 5V | 3.3V输出 | ⚠️ 需要三极管驱动 |
| LED | 取决于模块 | 3.3V输出 | ⚠️ 大功率需要驱动 |
| HW-269 | 5V | 3.3V输入 | ✅ 直接连接 |

### 接线顺序建议

**推荐接线顺序**（从简单到复杂）：
1. ✅ **电源线（3.3V、5V、GND）** - 最先接，确保供电正常
2. ✅ **I2C设备（OLED、MPU6050）** - 共享总线，容易调试
3. ✅ **简单GPIO（LED、蜂鸣器、红外）** - 单引脚，快速验证
4. ✅ **1-Wire设备（AM2302）** - 单数据线
5. ✅ **SPI设备（RC522）** - 多引脚，需要仔细核对
6. ✅ **HC-SR04** - 最后接，避免误触发

### 调试技巧

**逐个模块测试**：
1. 接好一个模块
2. 运行对应的测试代码
3. 确认工作正常
4. 再接下一个模块

**不要一次性全部接好**！
- ❌ 一次接所有模块 → 出错难排查
- ✅ 逐个接线测试 → 问题立即发现

---

## 接线示意图

### 完整接线图（面包板版）

```
                                ESP32开发板
                        ┌───────────────────────┐
                        │                       │
       I2C总线────────► │ GPIO21 (SDA)          │
                        │ GPIO22 (SCL)          │ ◄─────OLED + MPU6050
                        │                       │
       SPI总线────────► │ GPIO23 (MOSI)         │
                        │ GPIO19 (MISO)         │
                        │ GPIO18 (SCK)          │ ◄─────RC522 RFID
                        │ GPIO5  (CS)           │
                        │ GPIO4  (RST)          │
                        │                       │
       传感器 ────────► │ GPIO15 (AM2302 DATA)  │
                        │ GPIO25 (SR04 Trig)    │
                        │ GPIO26 (SR04 Echo)    │
                        │ GPIO33 (HW-269 OUT)   │
                        │                       │
       输出控制────────► │ GPIO27 (蜂鸣器)        │
                        │ GPIO32 (LED PWM)      │
                        │                       │
       电源 ──────────► │ 5V, 3.3V, GND        │
                        │                       │
                        └───────────────────────┘
```

### 面包板布局建议

```
面包板布局（俯视图）：

┌──────────────────────────────────────────────────────┐
│  电源轨（红色）：5V                                    │
├──────────────────────────────────────────────────────┤
│  电源轨（蓝色）：GND                                   │
├──────────────────────────────────────────────────────┤
│                                                      │
│  [ESP32开发板占位]────┬─────────────┐               │
│                        │             │               │
│                        │   功能区    │               │
│  [OLED]               │             │               │
│  [MPU6050]            │             │  [RC522]      │
│                        │  [AM2302]   │  [RFID]       │
│  [蜂鸣器]              │             │               │
│  [LED]                 │  [HC-SR04]  │               │
│                        │             │  [HW-269]     │
│                        └─────────────┘               │
│                                                      │
├──────────────────────────────────────────────────────┤
│  电源轨（蓝色）：GND                                   │
├──────────────────────────────────────────────────────┤
│  电源轨（红色）：3.3V                                  │
└──────────────────────────────────────────────────────┘
```

---

## 电源管理

### 电源需求分析

| 模块 | 工作电压 | 工作电流 | 峰值电流 |
|------|---------|---------|---------|
| ESP32 | 3.3V | 80mA | 240mA (WiFi/BLE) |
| OLED | 3.3V | 20mA | 20mA |
| MPU6050 | 3.3V | 3.5mA | 5mA |
| RC522 | 3.3V | 13mA | 26mA |
| AM2302 | 5V | 1mA | 1.5mA |
| HC-SR04 | 5V | 15mA | 15mA |
| 蜂鸣器 | 5V | 30mA | 30mA |
| LED (3W) | - | 取决于模块 | 可能>500mA |
| HW-269 | 5V | <50μA | <50μA |
| **总计** | - | **~163mA** | **~838mA** |

### 供电方案

**方案1：USB供电（推荐调试阶段）**
```
电脑USB ──► ESP32开发板 ──► 传感器模块
               │
               └──► 板载稳压器 (5V→3.3V)
```
- ✅ 优点：方便调试，可以同时烧录和供电
- ⚠️ 缺点：电流限制约500mA，大功率LED可能不够

**方案2：外部5V电源（推荐量产）**
```
5V电源适配器 ──► ESP32的VIN引脚 ──► 板载稳压器 ──► 3.3V
                        │
                        └──► 5V传感器（HC-SR04, AM2302等）
```
- ✅ 优点：电流充足（1A-2A）
- ⚠️ 注意：电源适配器质量要好，输出稳定

**方案3：电池供电（便携方案）**
```
3.7V锂电池 ──► 升压模块(3.7V→5V) ──► ESP32 VIN
或
2节18650电池(7.4V) ──► 降压模块→5V ──► ESP32 VIN
```

### 电源注意事项

- ⚠️ **RC522必须用3.3V**，接5V会烧毁！
- ⚠️ **不要把所有模块同时接5V和3.3V**，容易混乱
- ⚠️ **大功率LED单独供电**，不要共用ESP32的电源
- ✅ **所有GND必须连在一起**（共地）

---

## 常见接线错误

### 错误1：电源电压错误

❌ **错误**：RC522接到5V
```
RC522 VCC ──► ESP32 5V  ❌ 会烧毁RC522！
```

✅ **正确**：RC522接到3.3V
```
RC522 VCC ──► ESP32 3.3V  ✅
```

### 错误2：I2C设备接线反

❌ **错误**：SDA和SCL接反
```
OLED SDA ──► ESP32 GPIO22  ❌
OLED SCL ──► ESP32 GPIO21  ❌
```

✅ **正确**：
```
OLED SDA ──► ESP32 GPIO21 (SDA)  ✅
OLED SCL ──► ESP32 GPIO22 (SCL)  ✅
```

### 错误3：SPI接线混乱

❌ **错误**：MOSI和MISO接反
```
RC522 MOSI ──► ESP32 GPIO19  ❌
RC522 MISO ──► ESP32 GPIO23  ❌
```

✅ **正确**：
```
RC522 MOSI ──► ESP32 GPIO23 (MOSI)  ✅
RC522 MISO ──► ESP32 GPIO19 (MISO)  ✅
```

**记忆技巧**：
- **MOSI** = Master Out, Slave In (主机输出，从机输入)
- **MISO** = Master In, Slave Out (主机输入，从机输出)

### 错误4：忘记共地

❌ **错误**：模块独立供电，没有连接GND
```
ESP32 GND ──► 不连接
传感器 GND ──► 独立电源GND  ❌ 信号不稳定！
```

✅ **正确**：所有设备GND连在一起
```
ESP32 GND ──┬──► 传感器1 GND
            ├──► 传感器2 GND
            └──► 外部电源GND  ✅
```

### 错误5：使用了禁用的引脚

❌ **错误**：使用GPIO6-11
```
传感器 ──► GPIO6  ❌ GPIO6连接Flash，禁止使用！
```

✅ **正确**：使用安全引脚
```
传感器 ──► GPIO15, GPIO25-27, GPIO32-33  ✅
```

---

## 🔍 接线检查清单

在上电前，请逐项检查：

- [ ] 所有3.3V设备接到3.3V（特别是RC522）
- [ ] 所有5V设备接到5V或VIN
- [ ] 所有GND连在一起（共地）
- [ ] I2C设备的SDA/SCL没有接反
- [ ] SPI设备的MOSI/MISO/SCK/CS正确连接
- [ ] 没有使用GPIO6-11（Flash专用）
- [ ] GPIO0（BOOT按钮）没有被拉低
- [ ] 杜邦线连接牢固，没有松动
- [ ] 检查是否有短路（VCC和GND不能碰到）
- [ ] 极性正确（LED、电解电容等有正负极）

✅ **检查完毕后再上电！**

---

## 🎓 下一步

接线完成后：
1. 📖 逐个运行模块测试代码
2. 🧪 从 [test_modules/01_OLED_Test](../test_modules/01_OLED_Test/README.md) 开始

---

**接线愉快！有问题随时检查本文档。🔌**
